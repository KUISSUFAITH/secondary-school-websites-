function Dashboard() {
  return (
    <section className="page-card">
      <h1>Student Dashboard</h1>
      <p>This is a protected page. Only logged-in users with a token can access it.</p>
    </section>
  );
}

export default Dashboard;
