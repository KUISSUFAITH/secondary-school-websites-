# VicssLimbe

Full-stack secondary school website system (academic project).

## Structure

```text
vicsslimbe/
├── client/
├── server/
└── README.md
```

## Setup

1. Install dependencies:
   - `cd client && npm install`
   - `cd ../server && npm install`
2. Create backend env file:
   - copy `server/.env.example` to `server/.env`
3. Run apps:
   - frontend: `cd client && npm run dev`
   - backend: `cd server && npm run dev`

## Section 0 Status

- Root structure created
- React + Vite scaffold prepared
- Node + Express scaffold prepared
- Core dependencies declared in `package.json` files
